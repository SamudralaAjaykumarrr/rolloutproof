apiVersion: rolloutproof.dev/v1alpha1
kind: Schema
tables:
  - name: users
    columns:
      - name: id
        type: integer
        default: "nextval('id_seq'::regclass)"
      - name: referral_code
        type: text
        nullable: true
