apiVersion: rolloutproof.dev/v1beta1
kind: ServiceContract

service: orders
version: v2

api:
  provides:
    - contract: orders-api
      # DELETE /orders/:id has been removed entirely (SC-UNSAFE-007).
